# 📢 팀원들을 위한 "Hello World" 동작 원리

기본 템플릿은 **[브라우저 요청 → 컨트롤러 → JSP 화면]** 순서로 작동합니다.

<img width="549" height="672" alt="스프링 파일구조" src="https://github.com/user-attachments/assets/24ec2f92-cf18-438c-8bea-0e76c1149f51" />

> **💡 참고사항** > 처음 프로젝트를 내려받으시면 위 이미지와 구조가 조금 다를 수 있습니다. 이는 `.gitignore` 설정으로 인해 빌드 결과물이나 개인 설정 파일을 제외했기 때문입니다. **애플리케이션을 실행하면 필요한 파일들이 자동으로 생성되니** 안심하고 진행하세요.

---

### 1. HomeController.java (길잡이 역할)
* **위치**: `src/main/java/com/.../controller/`
* **역할**: 사용자가 브라우저 주소창에 특정 주소(URL)를 입력했을 때 어떤 화면을 보여줄지 결정하는 **안내원**입니다.

**주요 포인트:**
* `@Controller`: "이 클래스는 안내원이야"라고 스프링에게 알려주는 표식입니다.
* `@GetMapping("/")`: 사용자가 `localhost:8080/`으로 접속했을 때 이 메서드를 실행합니다.
* `return "home";`: `home.jsp` 파일을 찾아 화면에 띄웁니다.
    * *주의: 리턴값은 대소문자를 엄격히 구분하므로 JSP 파일명과 반드시 일치해야 합니다.*

---

### 2. home.jsp (실제 화면)
* **위치**: `src/main/webapp/WEB-INF/views/`
* **역할**: 사용자 눈에 보이는 **실제 웹 페이지**입니다. (VS Code의 HTML과 유사합니다.)

**주요 포인트:**
* HTML 태그를 자유롭게 사용하여 화면을 디자인합니다.
* **보안**: `WEB-INF` 폴더 안에 숨겨져 있어, 브라우저에서 직접 접근할 수 없으며 반드시 컨트롤러를 통해서만 볼 수 있습니다.

---

### 3. application.properties (설정 정보)
* **위치**: `src/main/resources/`
* **역할**: 프로젝트의 **환경 설정값**을 모아둔 곳입니다. (DB 연결 등 추후 설정이 추가될 예정입니다.)

**주요 포인트:**
* JSP 파일의 경로(`prefix`)와 확장자(`suffix`)가 정의되어 있어, 컨트롤러에서 파일 이름만으로 연결이 가능합니다.

---

### 4. SimplecontrollerApplication.java (메인 엔진)
* **위치**: 패키지 최상단
* **역할**: 프로젝트를 가동하는 **시동 파일**입니다.

**주요 포인트:**
* 상단의 **초록색 재생 버튼(▶️)**을 눌러 실행합니다.
* 일반 자바 클래스와 달리 콘솔에 결과가 바로 찍히지 않으며, 서버가 가동된 후 브라우저에서 `localhost:8080/`을 입력해야 화면이 표시됩니다.
* 이 파일이 실행되어야 **내장 서버(Tomcat)**가 켜지며 웹사이트 접속이 가능해집니다.
